extends layout

include mixins/_growthForm
include mixins/_growthListItem

block content
    .inner
        h2 #{title}
        .listitems
            each growth in growths
                    +growthListItem(growth)
        if h.isUserManager(user)
            h3= 'Add New Growth Target'
            +growthForm()
